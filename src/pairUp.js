var pairUp = function() {
  var currentShortestDistance;
  var currentShortestIndex;
  for (var i = 0; i < duplicateArray; i++) {
    // duplicate the array of nodes for the array called above
    currentDancer = duplicateArray[i];
  }
};